console.log('------------------FOR EACH LOOP---------------------');

// forEach loop 

let foods = ['bread','rice','meat','toor','pizza','Samosa'];

foods.forEach((food) => {
  console.log(food);
  
})


let arr = [1,2,3,4,5,6,7,8]; 

arr.forEach((num) => {
  console.log(num + ' => ' + num+1);
})

// forEach loop is usable for the array when you need to perform an action on each array element and don't need to break early.
